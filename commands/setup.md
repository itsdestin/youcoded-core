---
description: Run the toolkit setup wizard — installs dependencies, configures layers, personalizes your system
---

Run the setup-wizard skill to configure this toolkit installation. Walk the user through the full setup process: inventory their existing environment, resolve any conflicts, let them choose which layers to install, install dependencies, personalize templates, and verify everything works.
